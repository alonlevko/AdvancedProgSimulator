#ifndef ADVANCEDPROG_UTILS_H
#define ADVANCEDPROG_UTILS_H
#include <vector>
#include <string>
using namespace std;
// general functions we need to use in multiple places
vector<string> makeForParse(vector<string> vec);
bool isOperator(char c);
bool isComparator(char c);


#endif //ADVANCEDPROG_UTILS_H
